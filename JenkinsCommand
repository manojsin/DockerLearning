jenkins basic command 
Unix-based
You installed Jenkins on a Debian-based or a Fedora-based distribution, you can use the following commands:

$ sudo service jenkins restart
$ sudo service jenkins stop
$ sudo service jenkins start
Or in the latest distribution of Linux:

$ sudo systemctl start jenkins.service
$ sudo systemctl stop jenkins.service
$ sudo systemctl restart jenkins.service
 docker base
 
1.pull jenkins docker image from docker hub
   sudo docker pull jenkins/jenkins
2. run jenkins in docker container
   docker run -p 8080:8080 --name=jenkins-master jenkins/jenkins
   
docker pipline script reference
https://dzone.com/articles/building-docker-images-to-docker-hub-using-jenkins
